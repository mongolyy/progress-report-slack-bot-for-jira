{
  "name": "progress-report-slack-bot-for-jira",
  "version": "0.9.0",
  "description": "Progress report tool for Jira",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "local": "node_modules/.bin/nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/app.ts",
    "start": "npm run build && node dist/app.js"
  },
  "author": "mongolyy",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.3.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^15.6.0",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "typescript": "^4.2.4"
  }
}
